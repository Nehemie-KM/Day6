function addCarToAgent (secret, brand, model, color, power){ 

    if ( bran || mode || colo || powe ) { 
           console.log("one of the keys is missing or uncorrectly typed")
           return null
        }
        
         secret.car = { 
          bran: brand,
          mode: model, 
          colo: color, 
          powe: power

     }
     return secret
    }
        let secret = { 
            age : 57,
            code: "007",
            firstname: "James",
            lastname: "Bond"
}
 shareThatBeauty(addCarToAgent(secret, 'Larien','Naruto','black','14 HP') ) 